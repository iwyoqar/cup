# Base URL of the CUP backend API (public configuration only — never put secrets here). localhost is
# fine for local dev; production must be the real deployed backend's HTTPS URL, set at BUILD time
# (Vite bakes this in — changing it later requires a rebuild + redeploy of this app).
VITE_API_BASE_URL="http://localhost:3000"
